---
description: 本机 RTX 5080 计算硬约束
alwaysApply: true
---

# 本机计算约束

- **硬件**：1× RTX 5080（16GB）；用途：`fast` 冒烟与调试。
- **Python**：见 rule「Python 虚拟环境」。
- **GPU 互斥**：同时只跑一个占 GPU 的进程；可停本会话拉起的作业，勿杀用户自启进程。
- **推理/评测**：generate / eval / 调试推理**只在本机**；远端只训不测。
- 调度与拉 checkpoint 流程见 skill `train-ops` / `sync-ovan-server` / `generate`。
