---
description: 远端 Slurm（ovan-server / cls1）计算硬约束
alwaysApply: true
---

# 远端计算约束

## 集群

- 主机：`ssh ovan-server`；**仅** `partition=cls1`。
- `cls1-srv1`：8× A6000（48GB）；`cls1-srv[2-4]`：8× RTX 4090（24GB）。
- AI 训练：最多 **1 node、4 GPU**；禁止跨节点。

## 远端状态（强制前置）

在本机对远端做**作业相关操作**之前（`sbatch` / `slurm/sbatch-train.sh` / `scancel`、改 `temp/agent` 登记等），**必须先**调用：

```bash
bash scripts/remote_status.sh          # 或加 --json
```

未跑完、未根据输出确认 GPU/队列/登记可继续之前，不得提交或取消作业。只读拉日志可用 `tail_remote_logs`（见 skill `train-ops`），不替代本工具。

## 提交与登录节点

- 占 CPU/GPU 的工作必须经 Slurm；登录节点禁止非 Slurm 的重 Python 作业。
- 登录节点只做轻量事：队列/状态、只读日志、写 `temp/` 登记。
- 训练提交：`bash slurm/sbatch-train.sh <name>`（脚本在 `scripts/train/`；模板 `slurm/prototype.slurm`）。
- **禁止** AI 提交 `slurm/preprocess-*`。

## 文件系统

- 项目树只读；改代码只在本地 → `bash scripts/sync-ovan-server.sh push` → 再提交。
- 远端 AI **唯一可写**：`~/source/bdelf/temp/`（agent 登记；push/pull 均不同步）。
- `slurm/gpu_availability.py` / `tail_remote_logs.py` 内不得 SSH；本机汇总状态用 `scripts/remote_status.sh`。

## 互斥与禁区

- 同时只跑一个本 agent 已登记任务；可 `scancel` 自己的；勿动他人作业。
- 禁止在远端 generate / eval / 看效果；拉回本机测。
- 流程细节：skill `train-ops`、`sync-ovan-server`。
