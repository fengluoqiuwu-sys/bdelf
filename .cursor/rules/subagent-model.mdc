---
description: 三类 subagent 模型须用户指定、写入每层 Task prompt 并原样向内传；禁默认与 fast
alwaysApply: true
---

# Subagent 模型

开 **research / research-high / ingest** 类 Task **之前**，用户必须指定三类各用什么模型。  
**禁止默认、禁止猜测、禁止用主模型顶替**（除非用户明确写 `inherit`）。缺任一类 → **停下来问**，写入后再启动。

一律禁止 `*-fast`。用户点了 fast → 拒绝并再问。指定的模型不可用 → 再问，禁止改用 fast 或擅自换。

## 三类（按任务选 `model`）

| 类型 | 何时 | 现有 Task |
|------|------|-----------|
| **research** | 普通调查 | brainstorm、idea-explore、**potential**、survey、spec、proposal |
| **research-high** | 高难度（数学原理等） | critic |
| **ingest** | 查文献并总结 | paper-ingest |

`inherit` 仅当用户点名且主 agent 非 fast。

## 逐层传递（开 Task 强制）

每开一层 Task，**prompt 必须原文写入**下面整块（三类取值 + **禁止 fast** 都要有）。禁止只给 README / `stage.md` 路径代替取值。子代理再开更内层时**原样写入**（只改「本 Task 类型」），不得改值、不得省略禁止 fast。未收到完整块（缺取值或缺禁止 fast）→ **不准**开更内层 Task。

```markdown
## subagent 模型（原样向内传，禁止改、禁止省）
- 本 Task 类型: research | research-high | ingest
- research: …
- research-high: …
- ingest: …
- 禁止 fast: 一律禁止 `*-fast`；点了 fast 或指定模型不可用 → 拒绝并再问，禁止改用 fast 或擅自换
```

## 落盘

- scout：`temp/research-scout/<run>/README.md` 必填三项，并写上禁止 fast；开 Task 仍须把整块（含禁止 fast）写入 prompt。
- 开题：抄 scout README 或问用户，写入 `stage.md`；开 Task 同样写入整块（含禁止 fast）。
- 单独 ingest：未指定则先问。
