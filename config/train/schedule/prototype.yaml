_doc: Schedule template
name: prototype
variant: fast
max_steps: 5000
warmup_steps: 500
min_lr_ratio: 0.1
log_plot_every: 100
eval_every: 500
save_every: 2000
snapshot_every: 10000
resume: true
seed: 42
# Hidden Linear weights use Muon; others stay AdamW.
# Checkpoints are saved under <run-name>-muon (e.g. ar-100m-fast-muon).
use_muon: true
