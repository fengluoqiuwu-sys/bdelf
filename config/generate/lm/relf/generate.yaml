# =============================================================================
# 生成 / 采样（generate=正式生成；eval=训练在线 gen-eval / 测试）
# =============================================================================
_doc: RELF 正式生成采样  # 文件说明，写入 extra
name: generate  # 配置名；非 prototype 时与文件名一致
commit_x0hat: false  # 是否把 x0_hat 提交进下一窗（关则再 encode）
sampling_method: sde  # 采样方法：sde | ode
sde_gamma: 1.5  # SDE churn 系数；末流关闭
w_sc: 2.0  # 推理 self-cond CFG 缩放；sc_cfg=false 时忽略
w_ctx: 1.0  # 推理上下文 CFG；1 表示不丢前缀
temperature: 0.0  # 采样温度；≤0 为 argmax
